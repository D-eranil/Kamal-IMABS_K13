@using IMABS.Components.InlineEditors.TextEditor
@using IMABS.Components.Widgets.TextWidget
@using Kentico.PageBuilder.Web.Mvc
@model IMABS.Components.Widgets.TextWidget.TextWidgetViewModel

@if (Context.Kentico().PageBuilder().EditMode)
{
    //Html.RenderPartial("~/Components/InlineEditors/TextEditor/_TextEditor.cshtml", new TextEditorViewModel
    //{
    //    PropertyName = nameof(TextWidgetProperties.Text),
    //    Text = Model.Text
    //});
    <partial name="~/Components/InlineEditors/TextEditor/_TextEditor.cshtml"
             model="new TextEditorViewModel
                        {
                            PropertyName = nameof(TextWidgetProperties.Text),
                            Text = Model.Text,
                        }" />
}
else
{
    @Html.Raw(Model.Text);
}


